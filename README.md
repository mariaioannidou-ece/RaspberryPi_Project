# RaspberryPi_Project
Real-time multithreaded telemetry system in C for Raspberry Pi. Captures and logs data from the Jetstream API using a Producer-Consumer architecture (pthreads). Features network resilience, zero packet loss, and includes a 24-hour performance dataset.
